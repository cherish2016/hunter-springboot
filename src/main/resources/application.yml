server:
  port: 9090
  address: localhost

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hunter?useUnicode=true&characterEncoding=utf8
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    serialization: true

logging:
  level:
    org:
      springframework:
        web: ERROR
      hibernate: ERROR

filePath: E:\hunterdao\home
produceThreadNum: 5